
UseGroundColor = pick up color from ground, default is 1, set to 0 to use texture colors.

Can now specify ADD blend mode (blend = add).

ColorFade = multiplier for color when picked up from ground.

Gravity = scalar for gravity, 1.0 is normal gravity reaction.

